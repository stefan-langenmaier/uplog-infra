# Infrastructure scripts for uplog

## infra

creates necessary docer networks


## proxy

`docker-compose up -d proxy`

individual certificates need to be extended manually

## others

`docker-compose up -d`
